const cards=[...document.querySelectorAll(".card")],search=document.getElementById("search"),count=document.getElementById("count"),chips=document.getElementById("chips");
const genres=["All",...new Set(cards.map(c=>c.dataset.genre))];chips.innerHTML=genres.map(g=>`<button data-g="${g}" class="${g==="All"?"active":""}">${g}</button>`).join("");
let genre="All";function filter(){const q=search.value.toLowerCase().trim();let n=0;cards.forEach(c=>{const ok=(genre==="All"||c.dataset.genre===genre)&&c.innerText.toLowerCase().includes(q);c.style.display=ok?"":"none";if(ok)n++});count.textContent=n+" movies"}chips.querySelectorAll("button").forEach(b=>b.onclick=()=>{genre=b.dataset.g;chips.querySelectorAll("button").forEach(x=>x.classList.remove("active"));b.classList.add("active");filter()});search.oninput=filter;filter();